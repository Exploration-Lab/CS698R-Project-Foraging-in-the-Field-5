from setuptools import setup

setup(
    name='my_env',  # Use same name as sibling directory.
    version='1.0.0',  # Version number.
    install_requires=['gym']  # Python packages required by our environments.
)
